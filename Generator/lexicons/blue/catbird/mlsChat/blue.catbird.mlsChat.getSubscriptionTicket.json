{
  "lexicon": 1,
  "id": "blue.catbird.mlsChat.getSubscriptionTicket",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Get a short-lived signed ticket for subscribing to MLS events via WebSocket. The ticket is valid for 30 seconds and must be used to establish a WebSocket connection.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {
            "convoIds": {
              "type": "array",
              "description": "Optional: limit subscription to specific conversations. If omitted, subscribes to all conversations the user is a member of.",
              "items": {
                "type": "string"
              }
            }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["ticket", "endpoint", "expiresAt"],
          "properties": {
            "ticket": {
              "type": "string",
              "description": "Signed subscription ticket (JWT) to use as query parameter when connecting to WebSocket"
            },
            "endpoint": {
              "type": "string",
              "description": "WebSocket endpoint URL to connect to (e.g., wss://mls.catbird.blue/xrpc/blue.catbird.mlsChat.subscribeConvoEvents)"
            },
            "expiresAt": {
              "type": "string",
              "format": "datetime",
              "description": "When the ticket expires (ISO 8601). Must connect before this time."
            }
          }
        }
      },
      "errors": [
        {
          "name": "Unauthorized",
          "description": "Authentication required"
        },
        {
          "name": "Forbidden",
          "description": "User is not a member of the specified conversation"
        }
      ]
    }
  }
}
