@Serializable
enum class {{ enum_name }} {
    {% for constant in constants %}
    @SerialName("{{ constant.value }}")
    {{ constant.name }}{% if not loop.last %},
    {% endif %}
    {% endfor %}
}
