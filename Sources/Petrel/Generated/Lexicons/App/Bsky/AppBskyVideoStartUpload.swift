// Generated by Petrel. DO NOT EDIT.
import Foundation

// lexicon: 1, id: app.bsky.video.startUpload

public enum AppBskyVideoStartUpload {
    public static let typeIdentifier = "app.bsky.video.startUpload"
    public struct Input: ATProtocolCodable {
        public let sizeBytes: Int
        public let mimeType: String
        public let name: String?
        public let durationMs: Int?
        public let width: Int?
        public let height: Int?

        /// Standard public initializer
        public init(sizeBytes: Int, mimeType: String, name: String? = nil, durationMs: Int? = nil, width: Int? = nil, height: Int? = nil) {
            self.sizeBytes = sizeBytes
            self.mimeType = mimeType
            self.name = name
            self.durationMs = durationMs
            self.width = width
            self.height = height
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            sizeBytes = try container.decode(Int.self, forKey: .sizeBytes)
            mimeType = try container.decode(String.self, forKey: .mimeType)
            name = try container.decodeIfPresent(String.self, forKey: .name)
            durationMs = try container.decodeIfPresent(Int.self, forKey: .durationMs)
            width = try container.decodeIfPresent(Int.self, forKey: .width)
            height = try container.decodeIfPresent(Int.self, forKey: .height)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(sizeBytes, forKey: .sizeBytes)
            try container.encode(mimeType, forKey: .mimeType)
            try container.encodeIfPresent(name, forKey: .name)
            try container.encodeIfPresent(durationMs, forKey: .durationMs)
            try container.encodeIfPresent(width, forKey: .width)
            try container.encodeIfPresent(height, forKey: .height)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            let sizeBytesValue = try sizeBytes.toCBORValue()
            map = map.adding(key: "sizeBytes", value: sizeBytesValue)
            let mimeTypeValue = try mimeType.toCBORValue()
            map = map.adding(key: "mimeType", value: mimeTypeValue)
            if let value = name {
                let nameValue = try value.toCBORValue()
                map = map.adding(key: "name", value: nameValue)
            }
            if let value = durationMs {
                let durationMsValue = try value.toCBORValue()
                map = map.adding(key: "durationMs", value: durationMsValue)
            }
            if let value = width {
                let widthValue = try value.toCBORValue()
                map = map.adding(key: "width", value: widthValue)
            }
            if let value = height {
                let heightValue = try value.toCBORValue()
                map = map.adding(key: "height", value: heightValue)
            }
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case sizeBytes
            case mimeType
            case name
            case durationMs
            case width
            case height
        }
    }

    public struct Output: ATProtocolCodable {
        public let jobId: String

        public let partSizeBytes: Int

        public let partCount: Int

        public let expiresAt: ATProtocolDate

        /// Standard public initializer
        public init(
            jobId: String,

            partSizeBytes: Int,

            partCount: Int,

            expiresAt: ATProtocolDate

        ) {
            self.jobId = jobId

            self.partSizeBytes = partSizeBytes

            self.partCount = partCount

            self.expiresAt = expiresAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)

            jobId = try container.decode(String.self, forKey: .jobId)

            partSizeBytes = try container.decode(Int.self, forKey: .partSizeBytes)

            partCount = try container.decode(Int.self, forKey: .partCount)

            expiresAt = try container.decode(ATProtocolDate.self, forKey: .expiresAt)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            try container.encode(jobId, forKey: .jobId)

            try container.encode(partSizeBytes, forKey: .partSizeBytes)

            try container.encode(partCount, forKey: .partCount)

            try container.encode(expiresAt, forKey: .expiresAt)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()

            let jobIdValue = try jobId.toCBORValue()
            map = map.adding(key: "jobId", value: jobIdValue)

            let partSizeBytesValue = try partSizeBytes.toCBORValue()
            map = map.adding(key: "partSizeBytes", value: partSizeBytesValue)

            let partCountValue = try partCount.toCBORValue()
            map = map.adding(key: "partCount", value: partCountValue)

            let expiresAtValue = try expiresAt.toCBORValue()
            map = map.adding(key: "expiresAt", value: expiresAtValue)

            return map
        }

        private enum CodingKeys: String, CodingKey {
            case jobId
            case partSizeBytes
            case partCount
            case expiresAt
        }
    }

    public enum Error: String, Swift.Error, ATProtoErrorType, CustomStringConvertible {
        /// The declared MIME type is not supported.
        case unsupportedContentType = "UnsupportedContentType"
        /// The exact file size exceeds the per-file cap or remaining daily byte allowance.
        case videoTooLarge = "VideoTooLarge"
        /// The advisory declared duration exceeds the limit.
        case videoTooLong = "VideoTooLong"
        /// The advisory declared dimensions have an unsupported aspect ratio.
        case badAspectRatio = "BadAspectRatio"
        /// The daily video or byte allowance, including active reservations, is exhausted.
        case dailyLimitExceeded = "DailyLimitExceeded"
        /// The account has reached its open multipart upload limit.
        case tooManyOpenUploads = "TooManyOpenUploads"
        /// The account is not permitted to upload video.
        case uploadForbidden = "UploadForbidden"
        /// The service is draining, at capacity, or temporarily unable to create the multipart upload.
        case serviceOverloaded = "ServiceOverloaded"
        public var description: String {
            return rawValue
        }

        public var errorName: String {
            return rawValue
        }
    }
}

public extension ATProtoClient.App.Bsky.Video {
    // MARK: - startUpload

    // Start a multipart video upload. The declared size is exact, while optional media properties are advisory and used only for early failure; the authoritative probe runs asynchronously after upload.
    //
    // - Parameter input: The input parameters for the request

    ///
    /// - Returns: A tuple containing the HTTP response code and the decoded response data
    /// - Throws: NetworkError if the request fails or the response cannot be processed
    func startUpload(
        input: AppBskyVideoStartUpload.Input

    ) async throws -> (responseCode: Int, data: AppBskyVideoStartUpload.Output?) {
        let endpoint = "app.bsky.video.startUpload"

        var headers: [String: String] = [:]

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        let requestData: Data? = try JSONEncoder().encode(input)

        let queryItems: [URLQueryItem]? = nil

        let urlRequest = try await networkService.createURLRequest(
            endpoint: endpoint,
            method: "POST",
            headers: headers,
            body: requestData,
            queryItems: queryItems
        )

        // Determine service DID for this endpoint
        let serviceDID = await networkService.getServiceDID(for: "app.bsky.video.startUpload")
        let proxyHeaders = serviceDID.map { ["atproto-proxy": $0] }
        let (responseData, response) = try await networkService.performRequestReturningHTTPErrorResponses(urlRequest, skipTokenRefresh: false, additionalHeaders: proxyHeaders)
        let responseCode = response.statusCode

        // Only validate Content-Type and decode on success. Error responses
        // (4xx/5xx) may have missing or different Content-Type headers and
        // are handled by the caller via the status code.
        if (200 ... 299).contains(responseCode) {
            guard let contentType = response.allHeaderFields["Content-Type"] as? String else {
                throw NetworkError.invalidContentType(expected: "application/json", actual: "nil")
            }

            if !contentType.lowercased().contains("application/json") {
                throw NetworkError.invalidContentType(expected: "application/json", actual: contentType)
            }

            do {
                let decoder = JSONDecoder()
                let decodedData = try decoder.decode(AppBskyVideoStartUpload.Output.self, from: responseData)

                return (responseCode, decodedData)
            } catch {
                // Log the decoding error for debugging but still return the response code
                LogManager.logError("Failed to decode successful response for app.bsky.video.startUpload: \(error)")
                return (responseCode, nil)
            }
        } else {
            // Try to parse a declared structured error response
            if let atprotoError = ATProtoErrorParser.parse(
                data: responseData,
                statusCode: responseCode,
                errorType: AppBskyVideoStartUpload.Error.self
            ) {
                throw atprotoError
            }

            // Don't try to decode unknown or malformed error responses as success types
            return (responseCode, nil)
        }
    }
}
