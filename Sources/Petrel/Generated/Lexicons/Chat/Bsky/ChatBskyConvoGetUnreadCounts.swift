// Generated by Petrel. DO NOT EDIT.
import Foundation

// lexicon: 1, id: chat.bsky.convo.getUnreadCounts

public enum ChatBskyConvoGetUnreadCounts {
    public static let typeIdentifier = "chat.bsky.convo.getUnreadCounts"
    public struct Parameters: Parametrizable {
        public let includeGroupChats: Bool?

        public init(
            includeGroupChats: Bool? = nil
        ) {
            self.includeGroupChats = includeGroupChats
        }
    }

    public struct Output: ATProtocolCodable {
        public let unreadAcceptedConvos: Int

        public let unreadRequestConvos: Int

        /// Standard public initializer
        public init(
            unreadAcceptedConvos: Int,

            unreadRequestConvos: Int

        ) {
            self.unreadAcceptedConvos = unreadAcceptedConvos

            self.unreadRequestConvos = unreadRequestConvos
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)

            unreadAcceptedConvos = try container.decode(Int.self, forKey: .unreadAcceptedConvos)

            unreadRequestConvos = try container.decode(Int.self, forKey: .unreadRequestConvos)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            try container.encode(unreadAcceptedConvos, forKey: .unreadAcceptedConvos)

            try container.encode(unreadRequestConvos, forKey: .unreadRequestConvos)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()

            let unreadAcceptedConvosValue = try unreadAcceptedConvos.toCBORValue()
            map = map.adding(key: "unreadAcceptedConvos", value: unreadAcceptedConvosValue)

            let unreadRequestConvosValue = try unreadRequestConvos.toCBORValue()
            map = map.adding(key: "unreadRequestConvos", value: unreadRequestConvosValue)

            return map
        }

        private enum CodingKeys: String, CodingKey {
            case unreadAcceptedConvos
            case unreadRequestConvos
        }
    }
}

public extension ATProtoClient.Chat.Bsky.Convo {
    // MARK: - getUnreadCounts

    /// Returns unread conversation counts for conversations that are unlocked, not muted, split by convo status. Direct convos are excluded when a block relationship exists between the actor and the other member, or when the other member's account is deleted or deactivated. Group convos are considered unread if they have unread join request counts.
    ///
    /// - Parameter input: The input parameters for the request
    ///
    /// - Returns: A tuple containing the HTTP response code and the decoded response data
    /// - Throws: NetworkError if the request fails or the response cannot be processed
    func getUnreadCounts(input: ChatBskyConvoGetUnreadCounts.Parameters) async throws -> (responseCode: Int, data: ChatBskyConvoGetUnreadCounts.Output?) {
        let endpoint = "chat.bsky.convo.getUnreadCounts"

        let queryItems = input.asQueryItems()

        let urlRequest = try await networkService.createURLRequest(
            endpoint: endpoint,
            method: "GET",
            headers: ["Accept": "application/json"],
            body: nil,
            queryItems: queryItems
        )

        // Determine service DID for this endpoint
        let serviceDID = await networkService.getServiceDID(for: "chat.bsky.convo.getUnreadCounts")
        let proxyHeaders = serviceDID.map { ["atproto-proxy": $0] }
        let (responseData, response) = try await networkService.performRequest(urlRequest, skipTokenRefresh: false, additionalHeaders: proxyHeaders)
        let responseCode = response.statusCode

        // Only validate Content-Type and decode on success. Error responses
        // (4xx/5xx) may have missing or different Content-Type headers and
        // are handled via the status code / structured error parser below.
        if (200 ... 299).contains(responseCode) {
            guard let contentType = response.allHeaderFields["Content-Type"] as? String else {
                throw NetworkError.invalidContentType(expected: "application/json", actual: "nil")
            }

            if !contentType.lowercased().contains("application/json") {
                throw NetworkError.invalidContentType(expected: "application/json", actual: contentType)
            }

            do {
                let decoder = JSONDecoder()
                let decodedData = try decoder.decode(ChatBskyConvoGetUnreadCounts.Output.self, from: responseData)

                return (responseCode, decodedData)
            } catch {
                // Log the decoding error for debugging but still return the response code
                LogManager.logError("Failed to decode successful response for chat.bsky.convo.getUnreadCounts: \(error)")
                return (responseCode, nil)
            }
        } else {
            // If we can't parse a structured error, return the response code
            // (maintains backward compatibility for endpoints without defined errors)
            return (responseCode, nil)
        }
    }
}
