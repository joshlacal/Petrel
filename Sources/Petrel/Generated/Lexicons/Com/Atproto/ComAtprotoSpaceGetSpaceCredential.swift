// Generated by Petrel. DO NOT EDIT.
import Foundation

// lexicon: 1, id: com.atproto.space.getSpaceCredential

public enum ComAtprotoSpaceGetSpaceCredential {
    public static let typeIdentifier = "com.atproto.space.getSpaceCredential"
    public struct Input: ATProtocolCodable {
        public let space: SpaceRef
        public let clientAttestation: String?

        /// Standard public initializer
        public init(space: SpaceRef, clientAttestation: String? = nil) {
            self.space = space
            self.clientAttestation = clientAttestation
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            space = try container.decode(SpaceRef.self, forKey: .space)
            if container.contains(.clientAttestation) {
                guard try !container.decodeNil(forKey: .clientAttestation) else {
                    throw DecodingError.valueNotFound(
                        String.self,
                        DecodingError.Context(
                            codingPath: container.codingPath,
                            debugDescription: "Property 'clientAttestation' must not be null"
                        )
                    )
                }
                clientAttestation = try container.decode(String.self, forKey: .clientAttestation)
            } else {
                clientAttestation = nil
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(space, forKey: .space)
            try container.encodeIfPresent(clientAttestation, forKey: .clientAttestation)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            let spaceValue = try space.toCBORValue()
            map = map.adding(key: "space", value: spaceValue)
            if let value = clientAttestation {
                let clientAttestationValue = try value.toCBORValue()
                map = map.adding(key: "clientAttestation", value: clientAttestationValue)
            }
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case space
            case clientAttestation
        }
    }

    public struct Output: ATProtocolCodable {
        public let credential: String

        /// Standard public initializer
        public init(
            credential: String

        ) {
            self.credential = credential
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)

            credential = try container.decode(String.self, forKey: .credential)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            try container.encode(credential, forKey: .credential)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()

            let credentialValue = try credential.toCBORValue()
            map = map.adding(key: "credential", value: credentialValue)

            return map
        }

        private enum CodingKeys: String, CodingKey {
            case credential
        }
    }

    public enum Error: String, Swift.Error, ATProtoErrorType, CustomStringConvertible {
        case spaceNotFound = "SpaceNotFound"
        case spaceDeleted = "SpaceDeleted"
        /// Refused on the basis of the requesting user.
        case userNotAuthorized = "UserNotAuthorized"
        /// Refused on the basis of the requesting app.
        case appNotAuthorized = "AppNotAuthorized"
        /// Refused without attributing the refusal to the user or the app. Authorities that do not wish to disclose which perimeter failed may return this in place of UserNotAuthorized or AppNotAuthorized.
        case notAuthorized = "NotAuthorized"
        case invalidDelegationToken = "InvalidDelegationToken"
        case invalidClientAttestation = "InvalidClientAttestation"
        public var description: String {
            return rawValue
        }

        public var errorName: String {
            return rawValue
        }
    }

    public struct XRPCMethodDescriptor: Sendable, Equatable {
        public let nsid: String
        public let kind: String
        public let inputEncoding: String?
        public let outputEncoding: String?
        public let declaredErrors: [String]
    }

    public static let endpointDescriptor = XRPCMethodDescriptor(
        nsid: "com.atproto.space.getSpaceCredential", kind: "procedure",
        inputEncoding: "application/json", outputEncoding: "application/json",
        declaredErrors: ["SpaceNotFound", "SpaceDeleted", "UserNotAuthorized", "AppNotAuthorized", "NotAuthorized", "InvalidDelegationToken", "InvalidClientAttestation"]
    )

    public protocol ServerHandler: Sendable {
        associatedtype Context: Sendable
        func handle(
            parameters: Void, input: Input, context: Context
        ) async throws -> Output
    }
}

public extension ATProtoClient.Com.Atproto.Space {
    // MARK: - getSpaceCredential

    // Exchange a delegation token for a space credential. Called on the space authority, with the delegation token as the request's authorization token and a DPoP proof signed by the key to bind the credential to. The resulting space credential reads repos across the space. Requires a delegation token and DPoP proof, plus a client attestation when the space gates on app identity.
    //
    // - Parameter input: The input parameters for the request

    ///
    /// - Returns: A tuple containing the HTTP response code and the decoded response data
    /// - Throws: NetworkError if the request fails or the response cannot be processed
    func getSpaceCredential(
        input: ComAtprotoSpaceGetSpaceCredential.Input

    ) async throws -> (responseCode: Int, data: ComAtprotoSpaceGetSpaceCredential.Output?) {
        let endpoint = "com.atproto.space.getSpaceCredential"

        var headers: [String: String] = [:]

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        let requestData: Data? = try JSONEncoder().encode(input)

        let queryItems: [URLQueryItem]? = nil

        let urlRequest = try await networkService.createURLRequest(
            endpoint: endpoint,
            method: "POST",
            headers: headers,
            body: requestData,
            queryItems: queryItems
        )

        // Determine service DID for this endpoint
        let serviceDID = await networkService.getServiceDID(for: "com.atproto.space.getSpaceCredential")
        let proxyHeaders = serviceDID.map { ["atproto-proxy": $0] }
        let (responseData, response) = try await networkService.performRequestReturningHTTPErrorResponses(urlRequest, skipTokenRefresh: false, additionalHeaders: proxyHeaders)
        let responseCode = response.statusCode

        // Only validate Content-Type and decode on success. Error responses
        // (4xx/5xx) may have missing or different Content-Type headers and
        // are handled by the caller via the status code.
        if (200 ... 299).contains(responseCode) {
            guard let contentType = response.allHeaderFields["Content-Type"] as? String else {
                throw NetworkError.invalidContentType(expected: "application/json", actual: "nil")
            }

            if !contentType.lowercased().contains("application/json") {
                throw NetworkError.invalidContentType(expected: "application/json", actual: contentType)
            }

            do {
                let decoder = JSONDecoder()
                let decodedData = try decoder.decode(ComAtprotoSpaceGetSpaceCredential.Output.self, from: responseData)

                return (responseCode, decodedData)
            } catch {
                // Log the decoding error for debugging but still return the response code
                LogManager.logError("Failed to decode successful response for com.atproto.space.getSpaceCredential: \(error)")
                return (responseCode, nil)
            }
        } else {
            // Try to parse a declared structured error response
            if let atprotoError = ATProtoErrorParser.parse(
                data: responseData,
                statusCode: responseCode,
                errorType: ComAtprotoSpaceGetSpaceCredential.Error.self
            ) {
                throw atprotoError
            }

            if let genericError = ATProtoErrorParser.parseGeneric(data: responseData, statusCode: responseCode) {
                throw genericError
            }

            // Don't try to decode unknown or malformed error responses as success types
            return (responseCode, nil)
        }
    }
}
