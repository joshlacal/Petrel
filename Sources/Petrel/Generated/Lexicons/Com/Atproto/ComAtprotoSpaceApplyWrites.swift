// Generated by Petrel. DO NOT EDIT.
import Foundation

// lexicon: 1, id: com.atproto.space.applyWrites

public enum ComAtprotoSpaceApplyWrites {
    public static let typeIdentifier = "com.atproto.space.applyWrites"

    public struct Create: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "com.atproto.space.applyWrites#create"
        public let collection: NSID
        public let rkey: RecordKey?
        public let value: ATProtocolValueContainer

        public init(
            collection: NSID, rkey: RecordKey?, value: ATProtocolValueContainer
        ) {
            self.collection = collection
            self.rkey = rkey
            self.value = value
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                collection = try container.decode(NSID.self, forKey: .collection)
            } catch {
                LogManager.logError("Decoding error for required property 'collection': \(error)")
                throw error
            }
            do {
                rkey = try container.decodeIfPresent(RecordKey.self, forKey: .rkey)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'rkey' — degrading to nil: \(error)")
                rkey = nil
            }
            do {
                value = try container.decode(ATProtocolValueContainer.self, forKey: .value)
            } catch {
                LogManager.logError("Decoding error for required property 'value': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(collection, forKey: .collection)
            try container.encodeIfPresent(rkey, forKey: .rkey)
            try container.encode(value, forKey: .value)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(collection)
            if let value = rkey {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
            hasher.combine(value)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if collection != other.collection {
                return false
            }
            if rkey != other.rkey {
                return false
            }
            if value != other.value {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let collectionValue = try collection.toCBORValue()
            map = map.adding(key: "collection", value: collectionValue)
            if let value = rkey {
                let rkeyValue = try value.toCBORValue()
                map = map.adding(key: "rkey", value: rkeyValue)
            }
            let valueValue = try value.toCBORValue()
            map = map.adding(key: "value", value: valueValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case collection
            case rkey
            case value
        }
    }

    public struct Update: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "com.atproto.space.applyWrites#update"
        public let collection: NSID
        public let rkey: RecordKey
        public let value: ATProtocolValueContainer

        public init(
            collection: NSID, rkey: RecordKey, value: ATProtocolValueContainer
        ) {
            self.collection = collection
            self.rkey = rkey
            self.value = value
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                collection = try container.decode(NSID.self, forKey: .collection)
            } catch {
                LogManager.logError("Decoding error for required property 'collection': \(error)")
                throw error
            }
            do {
                rkey = try container.decode(RecordKey.self, forKey: .rkey)
            } catch {
                LogManager.logError("Decoding error for required property 'rkey': \(error)")
                throw error
            }
            do {
                value = try container.decode(ATProtocolValueContainer.self, forKey: .value)
            } catch {
                LogManager.logError("Decoding error for required property 'value': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(collection, forKey: .collection)
            try container.encode(rkey, forKey: .rkey)
            try container.encode(value, forKey: .value)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(collection)
            hasher.combine(rkey)
            hasher.combine(value)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if collection != other.collection {
                return false
            }
            if rkey != other.rkey {
                return false
            }
            if value != other.value {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let collectionValue = try collection.toCBORValue()
            map = map.adding(key: "collection", value: collectionValue)
            let rkeyValue = try rkey.toCBORValue()
            map = map.adding(key: "rkey", value: rkeyValue)
            let valueValue = try value.toCBORValue()
            map = map.adding(key: "value", value: valueValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case collection
            case rkey
            case value
        }
    }

    public struct Delete: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "com.atproto.space.applyWrites#delete"
        public let collection: NSID
        public let rkey: RecordKey

        public init(
            collection: NSID, rkey: RecordKey
        ) {
            self.collection = collection
            self.rkey = rkey
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                collection = try container.decode(NSID.self, forKey: .collection)
            } catch {
                LogManager.logError("Decoding error for required property 'collection': \(error)")
                throw error
            }
            do {
                rkey = try container.decode(RecordKey.self, forKey: .rkey)
            } catch {
                LogManager.logError("Decoding error for required property 'rkey': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(collection, forKey: .collection)
            try container.encode(rkey, forKey: .rkey)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(collection)
            hasher.combine(rkey)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if collection != other.collection {
                return false
            }
            if rkey != other.rkey {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let collectionValue = try collection.toCBORValue()
            map = map.adding(key: "collection", value: collectionValue)
            let rkeyValue = try rkey.toCBORValue()
            map = map.adding(key: "rkey", value: rkeyValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case collection
            case rkey
        }
    }

    public struct CreateResult: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "com.atproto.space.applyWrites#createResult"
        public let uri: ATProtocolURI
        public let cid: CID
        public let validationStatus: String?

        public init(
            uri: ATProtocolURI, cid: CID, validationStatus: String?
        ) {
            self.uri = uri
            self.cid = cid
            self.validationStatus = validationStatus
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                uri = try container.decode(ATProtocolURI.self, forKey: .uri)
            } catch {
                LogManager.logError("Decoding error for required property 'uri': \(error)")
                throw error
            }
            do {
                cid = try container.decode(CID.self, forKey: .cid)
            } catch {
                LogManager.logError("Decoding error for required property 'cid': \(error)")
                throw error
            }
            do {
                validationStatus = try container.decodeIfPresent(String.self, forKey: .validationStatus)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'validationStatus' — degrading to nil: \(error)")
                validationStatus = nil
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(uri, forKey: .uri)
            try container.encode(cid, forKey: .cid)
            try container.encodeIfPresent(validationStatus, forKey: .validationStatus)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(uri)
            hasher.combine(cid)
            if let value = validationStatus {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if uri != other.uri {
                return false
            }
            if cid != other.cid {
                return false
            }
            if validationStatus != other.validationStatus {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let uriValue = try uri.toCBORValue()
            map = map.adding(key: "uri", value: uriValue)
            let cidValue = try cid.toCBORValue()
            map = map.adding(key: "cid", value: cidValue)
            if let value = validationStatus {
                let validationStatusValue = try value.toCBORValue()
                map = map.adding(key: "validationStatus", value: validationStatusValue)
            }
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case uri
            case cid
            case validationStatus
        }
    }

    public struct UpdateResult: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "com.atproto.space.applyWrites#updateResult"
        public let uri: ATProtocolURI
        public let cid: CID
        public let validationStatus: String?

        public init(
            uri: ATProtocolURI, cid: CID, validationStatus: String?
        ) {
            self.uri = uri
            self.cid = cid
            self.validationStatus = validationStatus
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                uri = try container.decode(ATProtocolURI.self, forKey: .uri)
            } catch {
                LogManager.logError("Decoding error for required property 'uri': \(error)")
                throw error
            }
            do {
                cid = try container.decode(CID.self, forKey: .cid)
            } catch {
                LogManager.logError("Decoding error for required property 'cid': \(error)")
                throw error
            }
            do {
                validationStatus = try container.decodeIfPresent(String.self, forKey: .validationStatus)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'validationStatus' — degrading to nil: \(error)")
                validationStatus = nil
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(uri, forKey: .uri)
            try container.encode(cid, forKey: .cid)
            try container.encodeIfPresent(validationStatus, forKey: .validationStatus)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(uri)
            hasher.combine(cid)
            if let value = validationStatus {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if uri != other.uri {
                return false
            }
            if cid != other.cid {
                return false
            }
            if validationStatus != other.validationStatus {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let uriValue = try uri.toCBORValue()
            map = map.adding(key: "uri", value: uriValue)
            let cidValue = try cid.toCBORValue()
            map = map.adding(key: "cid", value: cidValue)
            if let value = validationStatus {
                let validationStatusValue = try value.toCBORValue()
                map = map.adding(key: "validationStatus", value: validationStatusValue)
            }
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case uri
            case cid
            case validationStatus
        }
    }

    public struct DeleteResult: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "com.atproto.space.applyWrites#deleteResult"

        public init() {}

        public init(from decoder: Decoder) throws {
            _ = decoder
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
        }

        public func hash(into hasher: inout Hasher) {}

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            return other is Self
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
        }
    }

    public struct Input: ATProtocolCodable {
        public let space: SpaceRef
        public let repo: DID
        public let validate: Bool?
        public let writes: [InputWritesUnion]

        /// Standard public initializer
        public init(space: SpaceRef, repo: DID, validate: Bool? = nil, writes: [InputWritesUnion]) {
            self.space = space
            self.repo = repo
            self.validate = validate
            self.writes = writes
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            space = try container.decode(SpaceRef.self, forKey: .space)
            repo = try container.decode(DID.self, forKey: .repo)
            validate = try container.decodeIfPresent(Bool.self, forKey: .validate)
            writes = try container.decode([InputWritesUnion].self, forKey: .writes)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(space, forKey: .space)
            try container.encode(repo, forKey: .repo)
            try container.encodeIfPresent(validate, forKey: .validate)
            try container.encode(writes, forKey: .writes)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            let spaceValue = try space.toCBORValue()
            map = map.adding(key: "space", value: spaceValue)
            let repoValue = try repo.toCBORValue()
            map = map.adding(key: "repo", value: repoValue)
            if let value = validate {
                let validateValue = try value.toCBORValue()
                map = map.adding(key: "validate", value: validateValue)
            }
            let writesValue = try writes.toCBORValue()
            map = map.adding(key: "writes", value: writesValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case space
            case repo
            case validate
            case writes
        }
    }

    public struct Output: ATProtocolCodable {
        public let results: [OutputResultsUnion]?

        /// Standard public initializer
        public init(
            results: [OutputResultsUnion]? = nil

        ) {
            self.results = results
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)

            do {
                results = try container.decodeIfPresent([OutputResultsUnion].self, forKey: .results)
            } catch {
                // Forward compatibility: a malformed optional field must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'results' — degrading to nil: \(error)")
                results = nil
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            // Encode optional property even if it's an empty array
            try container.encodeIfPresent(results, forKey: .results)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()

            if let value = results {
                // Encode optional property even if it's an empty array for CBOR
                let resultsValue = try value.toCBORValue()
                map = map.adding(key: "results", value: resultsValue)
            }

            return map
        }

        private enum CodingKeys: String, CodingKey {
            case results
        }
    }

    public enum Error: String, Swift.Error, ATProtoErrorType, CustomStringConvertible {
        case spaceNotFound = "SpaceNotFound"
        /// An update or delete targeted a record that does not exist.
        case recordNotFound = "RecordNotFound"
        /// A create targeted a collection and rkey that already holds a record.
        case recordAlreadyExists = "RecordAlreadyExists"
        public var description: String {
            return rawValue
        }

        public var errorName: String {
            return rawValue
        }
    }

    public enum InputWritesUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case comAtprotoSpaceApplyWritesCreate(ComAtprotoSpaceApplyWrites.Create)
        case comAtprotoSpaceApplyWritesUpdate(ComAtprotoSpaceApplyWrites.Update)
        case comAtprotoSpaceApplyWritesDelete(ComAtprotoSpaceApplyWrites.Delete)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: ComAtprotoSpaceApplyWrites.Create) {
            self = .comAtprotoSpaceApplyWritesCreate(value)
        }

        public init(_ value: ComAtprotoSpaceApplyWrites.Update) {
            self = .comAtprotoSpaceApplyWritesUpdate(value)
        }

        public init(_ value: ComAtprotoSpaceApplyWrites.Delete) {
            self = .comAtprotoSpaceApplyWritesDelete(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "com.atproto.space.applyWrites#create":
                let value = try ComAtprotoSpaceApplyWrites.Create(from: decoder)
                self = .comAtprotoSpaceApplyWritesCreate(value)
            case "com.atproto.space.applyWrites#update":
                let value = try ComAtprotoSpaceApplyWrites.Update(from: decoder)
                self = .comAtprotoSpaceApplyWritesUpdate(value)
            case "com.atproto.space.applyWrites#delete":
                let value = try ComAtprotoSpaceApplyWrites.Delete(from: decoder)
                self = .comAtprotoSpaceApplyWritesDelete(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .comAtprotoSpaceApplyWritesCreate(value):
                try container.encode("com.atproto.space.applyWrites#create", forKey: .type)
                try value.encode(to: encoder)
            case let .comAtprotoSpaceApplyWritesUpdate(value):
                try container.encode("com.atproto.space.applyWrites#update", forKey: .type)
                try value.encode(to: encoder)
            case let .comAtprotoSpaceApplyWritesDelete(value):
                try container.encode("com.atproto.space.applyWrites#delete", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .comAtprotoSpaceApplyWritesCreate(value):
                hasher.combine("com.atproto.space.applyWrites#create")
                hasher.combine(value)
            case let .comAtprotoSpaceApplyWritesUpdate(value):
                hasher.combine("com.atproto.space.applyWrites#update")
                hasher.combine(value)
            case let .comAtprotoSpaceApplyWritesDelete(value):
                hasher.combine("com.atproto.space.applyWrites#delete")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: InputWritesUnion, rhs: InputWritesUnion) -> Bool {
            switch (lhs, rhs) {
            case let (
                .comAtprotoSpaceApplyWritesCreate(lhsValue),
                .comAtprotoSpaceApplyWritesCreate(rhsValue)
            ):
                return lhsValue == rhsValue
            case let (
                .comAtprotoSpaceApplyWritesUpdate(lhsValue),
                .comAtprotoSpaceApplyWritesUpdate(rhsValue)
            ):
                return lhsValue == rhsValue
            case let (
                .comAtprotoSpaceApplyWritesDelete(lhsValue),
                .comAtprotoSpaceApplyWritesDelete(rhsValue)
            ):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? InputWritesUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .comAtprotoSpaceApplyWritesCreate(value):
                map = map.adding(key: "$type", value: "com.atproto.space.applyWrites#create")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .comAtprotoSpaceApplyWritesUpdate(value):
                map = map.adding(key: "$type", value: "com.atproto.space.applyWrites#update")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .comAtprotoSpaceApplyWritesDelete(value):
                map = map.adding(key: "$type", value: "com.atproto.space.applyWrites#delete")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum OutputResultsUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case comAtprotoSpaceApplyWritesCreateResult(ComAtprotoSpaceApplyWrites.CreateResult)
        case comAtprotoSpaceApplyWritesUpdateResult(ComAtprotoSpaceApplyWrites.UpdateResult)
        case comAtprotoSpaceApplyWritesDeleteResult(ComAtprotoSpaceApplyWrites.DeleteResult)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: ComAtprotoSpaceApplyWrites.CreateResult) {
            self = .comAtprotoSpaceApplyWritesCreateResult(value)
        }

        public init(_ value: ComAtprotoSpaceApplyWrites.UpdateResult) {
            self = .comAtprotoSpaceApplyWritesUpdateResult(value)
        }

        public init(_ value: ComAtprotoSpaceApplyWrites.DeleteResult) {
            self = .comAtprotoSpaceApplyWritesDeleteResult(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "com.atproto.space.applyWrites#createResult":
                let value = try ComAtprotoSpaceApplyWrites.CreateResult(from: decoder)
                self = .comAtprotoSpaceApplyWritesCreateResult(value)
            case "com.atproto.space.applyWrites#updateResult":
                let value = try ComAtprotoSpaceApplyWrites.UpdateResult(from: decoder)
                self = .comAtprotoSpaceApplyWritesUpdateResult(value)
            case "com.atproto.space.applyWrites#deleteResult":
                let value = try ComAtprotoSpaceApplyWrites.DeleteResult(from: decoder)
                self = .comAtprotoSpaceApplyWritesDeleteResult(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .comAtprotoSpaceApplyWritesCreateResult(value):
                try container.encode("com.atproto.space.applyWrites#createResult", forKey: .type)
                try value.encode(to: encoder)
            case let .comAtprotoSpaceApplyWritesUpdateResult(value):
                try container.encode("com.atproto.space.applyWrites#updateResult", forKey: .type)
                try value.encode(to: encoder)
            case let .comAtprotoSpaceApplyWritesDeleteResult(value):
                try container.encode("com.atproto.space.applyWrites#deleteResult", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .comAtprotoSpaceApplyWritesCreateResult(value):
                hasher.combine("com.atproto.space.applyWrites#createResult")
                hasher.combine(value)
            case let .comAtprotoSpaceApplyWritesUpdateResult(value):
                hasher.combine("com.atproto.space.applyWrites#updateResult")
                hasher.combine(value)
            case let .comAtprotoSpaceApplyWritesDeleteResult(value):
                hasher.combine("com.atproto.space.applyWrites#deleteResult")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: OutputResultsUnion, rhs: OutputResultsUnion) -> Bool {
            switch (lhs, rhs) {
            case let (
                .comAtprotoSpaceApplyWritesCreateResult(lhsValue),
                .comAtprotoSpaceApplyWritesCreateResult(rhsValue)
            ):
                return lhsValue == rhsValue
            case let (
                .comAtprotoSpaceApplyWritesUpdateResult(lhsValue),
                .comAtprotoSpaceApplyWritesUpdateResult(rhsValue)
            ):
                return lhsValue == rhsValue
            case let (
                .comAtprotoSpaceApplyWritesDeleteResult(lhsValue),
                .comAtprotoSpaceApplyWritesDeleteResult(rhsValue)
            ):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? OutputResultsUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .comAtprotoSpaceApplyWritesCreateResult(value):
                map = map.adding(key: "$type", value: "com.atproto.space.applyWrites#createResult")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .comAtprotoSpaceApplyWritesUpdateResult(value):
                map = map.adding(key: "$type", value: "com.atproto.space.applyWrites#updateResult")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .comAtprotoSpaceApplyWritesDeleteResult(value):
                map = map.adding(key: "$type", value: "com.atproto.space.applyWrites#deleteResult")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }
}

public extension ATProtoClient.Com.Atproto.Space {
    // MARK: - applyWrites

    // Apply a batch transaction of creates, updates, and deletes in a permissioned space. Requires auth, implemented by PDS.
    //
    // - Parameter input: The input parameters for the request

    ///
    /// - Returns: A tuple containing the HTTP response code and the decoded response data
    /// - Throws: NetworkError if the request fails or the response cannot be processed
    func applyWrites(
        input: ComAtprotoSpaceApplyWrites.Input

    ) async throws -> (responseCode: Int, data: ComAtprotoSpaceApplyWrites.Output?) {
        let endpoint = "com.atproto.space.applyWrites"

        var headers: [String: String] = [:]

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        let requestData: Data? = try JSONEncoder().encode(input)

        let queryItems: [URLQueryItem]? = nil

        let urlRequest = try await networkService.createURLRequest(
            endpoint: endpoint,
            method: "POST",
            headers: headers,
            body: requestData,
            queryItems: queryItems
        )

        // Determine service DID for this endpoint
        let serviceDID = await networkService.getServiceDID(for: "com.atproto.space.applyWrites")
        let proxyHeaders = serviceDID.map { ["atproto-proxy": $0] }
        let (responseData, response) = try await networkService.performRequestReturningHTTPErrorResponses(urlRequest, skipTokenRefresh: false, additionalHeaders: proxyHeaders)
        let responseCode = response.statusCode

        // Only validate Content-Type and decode on success. Error responses
        // (4xx/5xx) may have missing or different Content-Type headers and
        // are handled by the caller via the status code.
        if (200 ... 299).contains(responseCode) {
            guard let contentType = response.allHeaderFields["Content-Type"] as? String else {
                throw NetworkError.invalidContentType(expected: "application/json", actual: "nil")
            }

            if !contentType.lowercased().contains("application/json") {
                throw NetworkError.invalidContentType(expected: "application/json", actual: contentType)
            }

            do {
                let decoder = JSONDecoder()
                let decodedData = try decoder.decode(ComAtprotoSpaceApplyWrites.Output.self, from: responseData)

                return (responseCode, decodedData)
            } catch {
                // Log the decoding error for debugging but still return the response code
                LogManager.logError("Failed to decode successful response for com.atproto.space.applyWrites: \(error)")
                return (responseCode, nil)
            }
        } else {
            // Try to parse a declared structured error response
            if let atprotoError = ATProtoErrorParser.parse(
                data: responseData,
                statusCode: responseCode,
                errorType: ComAtprotoSpaceApplyWrites.Error.self
            ) {
                throw atprotoError
            }

            // Don't try to decode unknown or malformed error responses as success types
            return (responseCode, nil)
        }
    }
}
